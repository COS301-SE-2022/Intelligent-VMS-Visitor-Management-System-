import Layout from "../components/Layout";
import Hero from "../components/Hero";

const Home = () => {
    return (
        <Layout>
            <Hero />
        </Layout>
    );
};

export default Home;
